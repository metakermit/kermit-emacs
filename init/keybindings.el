;; my custom keybindings
;; - convention based on http://stackoverflow.com/a/5682758/544059

;; a more useful grep (only for versioned files)
(global-set-key (kbd "C-c g") (negative-argument projectile-grep))
